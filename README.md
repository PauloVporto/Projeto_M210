# 🧮 Projeto M210 – Método Simplex (Tableau)

Implementação **do zero** do método Simplex em Python, sem uso de bibliotecas de otimização.

## 🚀 Como rodar

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
```

Acesse `http://localhost:8501` e insira os coeficientes do problema.

## 📊 Saída
- Ponto ótimo (valores das variáveis)
- Lucro ótimo (Z)
- Preço-sombra das restrições
- Tableau final
